import 'package:flutter/material.dart';

class MaterialIdentificationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Material Identification'),
      ),
      body: Center(
        child: Text('AI model here '),
      ),
    );
  }
}

