import 'package:flutter/material.dart';


class HarajPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Haraj'),
      ),
      body: Center(
        child: Text('This is the Haraj page.'),
      ),
    );
  }
}